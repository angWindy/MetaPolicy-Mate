-r requirements.txt
datasets>=3,<5
# Ragas 0.4.3 imports a VertexAI compatibility module removed from the
# supported LangChain Community line. Keep the evaluated 0.3 API until the
# adapter is migrated and its dependency matrix is verified.
ragas>=0.3.3,<0.4
langchain-community>=0.3.27,<0.4
